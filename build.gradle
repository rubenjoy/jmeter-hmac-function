allprojects {
    repositories {
        jcenter()
    }

    apply plugin: 'java'

    dependencies {
        implementation 'org.apache.jmeter:ApacheJMeter_core:5.0'

        testImplementation 'junit:junit:4.12'
    }
}